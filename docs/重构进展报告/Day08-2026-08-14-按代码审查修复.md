# Day08-2026-08-14 · 按代码审查报告修复

> 依据 `docs/code-review-2026-08-14.md` 做一轮调整。#2（PIN 暴力破解防护）报告中标注"现阶段暂时忽略"，本轮不处理。
> 用户追加需求：README 补家长端启动方式。

---

## 1. 完成了什么

| 审查项 | 级别 | 处理 | 验证 |
|---|---|---|---|
| #1 确定性高危信号可被 LLM 分类降级覆盖 | 🔴 P0 | ✅ 已修：`core/risk_classifier.py` `classify()` 在采纳 LLM 结果前加确定性下限——Step 1 关键词命中给出 `_FALLBACK_MAP` 下限，LLM 只能抬高不能压低；抬升时同步修正 topic/action/needs_parent_alert 并在 reasoning 留痕 | 新增 2 条回归测试（单元层 + 端到端：LLM 误判 self_harm 为低风险仍触发危机模板，`test_pipeline.py`） |
| #2 PIN 无暴力破解防护 | 🔴 P0 | ⏸️ **按报告标注"现阶段忽略"跳过**（提交后建议：per-username 失败锁定 + 全局限流） | — |
| #3 CORS `*` + credentials | 🟡 P2 | ✅ 已修：`Settings.cors_origins`（`ANXIN_CORS_ORIGINS`，逗号分隔），默认仅 Vite 开发源；`main.py` 改显式白名单 | 36 tests 绿 |
| #4 静默吞异常无日志 | 🟡 P1 | ✅ 已修：5 处 `except Exception` 加 `logger.warning(..., exc_info=True)`——pipeline Step 5 生成、risk_classifier Step 2 分类、critic_agent Step 6 审计、episodic_memory 摘要、cocreation_service 话题卡 | 36 tests 绿 |
| #5 SQLite 并发写 | 🟡 P1 | ✅ 已修：`chat_service._with_write_retry()`（OperationalError "locked" 短退避重试 3 次），应用于情景记忆写回调与主请求 commit | 36 tests 绿 |
| #6 messages 无限膨胀 | 🟡 P2 | ✅ 已修：持久化时截断到最近 50 轮（`_MAX_PERSISTED_MESSAGES = 100`） | 36 tests 绿 |
| #7 `_next_id` 竞态 | 🟡 P2 | ✅ 已修：计数 ID 附短 UUID 后缀（仅影响 Streamlit 旧版 dict 路径；全栈 DB 自增不受影响） | 36 tests 绿 |
| #8 safety-demo 写全局 JSON | 🟡 P2 | ✅ 已修：演示路径改传空回调（不读不写全局 `episodic_memory.json`）；AGENTS.md 三处口径同步 | 36 tests 绿 |
| Nit：SSE 缓存头 | 💭 | ✅ `StreamingResponse` 加 `Cache-Control: no-cache` + `X-Accel-Buffering: no` | — |
| Nit：episodic 顺序注释 | 💭 | ✅ `retrieve_recent` 错误注释"最新在最后"改为"时间倒序（最新在前）"，与 docstring 一致 | — |
| Nit：`mincraft` 拼写 | 💭 | ⏸️ 保留：作为儿童常见误拼兼容，无危害 | — |
| Nit：隐私关键词误报（"几年级"等） | 💭 | ⏸️ 保留现状：临近提交冻结安全行为，宁紧勿松；提交后再评估"关键词+具体值"加权 | — |
| Nit：useChat 生产无日志 | 💭 | ⏸️ 跳过：需引入前端埋点基础设施，超出本轮范围 | — |

**附带完成**：

- README 新增「家长端怎么进」小节（全栈版同进程同端口、demo_parent 登录、儿童注册关联家长用户名；Streamlit 旧版 `parent_app.py`）；顺手同步"勇敢芽→探索芽"、pages 清单补 ChildHome
- 测试计数全仓同步 34 → 36（AGENTS.md / README.md / 方案书 / 提交工作方案）；`测试报告-2026-08-14.txt` 更新为 36 passed（替换 08-09 旧档）
- `AGENTS.md` §4 配置表新增 `ANXIN_CORS_ORIGINS`

**验证**：`python -m pytest backend/tests -q` → **36 passed**（34 + 2 新增回归）。本轮未动前端，`npm run build` 无回归风险（沿用上轮构建）。

## 2. 偏差

- #1 的修复比报告建议多做一点：抬升 risk_level 时同步把 `topic` 改为确定性命中的类别、`action` 重查 `_ACTION_MAP`、`needs_parent_alert` 按 `>=2` 重算——避免"risk 3 + topic safe"这类自相矛盾的分类结果流入策略引擎。
- #5 未改串行化/PostgreSQL，采用报告备选方案中的"写回调内加重试"（最小改动）。

## 3. 需要注意的事项

- **#2 PIN 暴力破解是刻意的已知风险**，提交材料不要宣称"防暴力破解"；部署侧可用 Nginx 限流临时兜底。提交后优先做：失败锁定 + slowapi 全局限流 + 家长账号强认证。
- CORS 白名单默认只放行 5173：若后续前后端分离部署，必须用 `ANXIN_CORS_ORIGINS` 配正式域名，否则前端跨域全挂。
- 危机模板链路现在有双保险（关键词确定性下限 + 危机模板），演示案例 4 在任何 LLM 状态下都必然触发——录屏/现场演示更稳了。
- `chat_sessions.messages` 截断为最近 50 轮：若有家长端功能依赖更早历史（当前没有），需知悉此上限。

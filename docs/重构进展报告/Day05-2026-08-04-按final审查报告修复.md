# Day 05 · 按 final 审查报告修复（Q2-Q13）+ 文档全量同步

> 日期：2026-08-04（重构第 5 天）
> 依据：`docs/CodeReview-2026-08-04-v2.1-final审查.md`
> 背景：v2.1-final 已完成架构调整（Pipeline 精简为 8 步、移除 Qwen3Guard、mode 自动识别、per-user 情景记忆），本日处理审查发现的全部阻断级/严重级问题

---

## 一、完成了什么

### 1.1 🔴 阻断级（Q2-Q5）

| 项 | 修复 |
|----|------|
| Q2 `.env` 未忽略 | `.gitignore` 增加 `.env` / `.env.local` / `.env.*.local` |
| Q3 情景记忆 JSON 未忽略 | `.gitignore` 增加 `data/episodic_memory.json` |
| Q4 node_modules 未忽略 | `.gitignore` 增加 `node_modules/` `**/node_modules/` + `frontend/dist/` |
| Q5 JWT 默认密钥无校验 | `backend/config.py` 增加 `jwt_using_default_secret()`；`scripts/start_server.py` 启动时醒目警告；`backend/main.py` lifespan 启动日志警告。选"警告"而非"启动失败"：评委无 .env 也要能跑通演示 |

### 1.2 🟡 严重级（Q6/Q8/Q9/Q10/Q13）

- **Q6 过时注释/文档（13 处全修）**：chat_service（Step 6/6c→Step 6）、main.py（per-user 已落地的新注释）、parent.py safety-demo 注释、start_server.py docstring、backend/requirements.txt（删 Qwen3Guard 注释）、demo_cases.py（5→7 个）、critic_agent.py（"流式片段"→"完整回复"）、app.py（Qwen3Guard pill→"策展式记忆"）、**README 全量重写**（SpaceMind 口径 + v2.1 优先 + 新结构 + 8 步 Pipeline + 旧版兜底）、deployment.md（删 SiliconFlow + 步骤编号 1-6 连续）、AGENTS.md（同步 8 步架构与新命令）、spike 验证报告（加"历史记录"弃用头注）
- **Q8 SQLite 锁竞争**：`database.py` 增加 WAL 模式 + busy_timeout=5000（connect 事件）
- **Q9** 随 Q6 修复（demo_cases 注释）
- **Q10 敏感词**：SafetyEngine placeholder"我想自杀"→"我不想活了"（与案例 4 一致）
- **Q13 死代码**：删除 `pipeline.run_async()`（无人调用，且内部也只是 run_in_executor）
- **Q16 检查**：`backend/models/__init__.py` 6 个模型全部显式导出，无需改

### 1.3 验证（全部实做）

- `python -m pytest backend/tests -q` → **22 passed**
- `python -c "from core import pipeline"` 导入正常（run_async 删除无影响）
- `app.py` 语法检查通过（Streamlit 兜底未破）
- `npm run build` 通过

## 二、偏差与遗留

| 项 | 状态 | 说明 |
|----|------|------|
| **Q1 v2.1 代码未入 git** | ⏳ **待产品负责人确认后提交** | git 变更类操作需显式确认；gitignore 已就位（敏感文件不会入库），提交计划见下方"下一步" |
| Q7 真实 LLM 路径无自动化测试 | 留决赛 | conftest 强制无 Key 是有意设计（评委环境可复现）；决赛补 VCR 回放或 monkeypatch 测试 |
| Q11 话题清单双端硬编码 | 留决赛 | 11 个话题稳定，demo 可接受 |
| Q12 `used_fallback` 语义混淆 | 留决赛 | 前端未依赖该字段判断"是否真实 AI"，暂无实际影响 |
| Q14 `check_abnormal_time` 用当前时间 | 留决赛 | 有 ts 可提取，优化项 |
| Q15 未解锁胶囊返回明文 | 留决赛 | 与旧 Streamlit 版同级，CSS blur 已挡"惊喜感"的主流场景 |

## 三、需要注意的事项

1. **Qwen3Guard 移除后的叙事一致性**：README/AGENTS.md/spike 报告/方案书（第十部分）/deployment.md 已全部对齐"8 步 Pipeline + 三层接力"口径；方案书正文（第一章~第九章）的 Qwen3Guard 叙事修订属于"方案书同步重写"工作流（P0，8/6-8/7）
2. **JWT 警告是故意非阻断的**：评委环境通常不配 JWT_SECRET，启动会打印警告但能跑——提交材料里不要对这条警告感到意外；正式部署必须设置（deployment.md 已强调）
3. **frontend/dist/ 也入了 gitignore**：部署机需要 `npm run build`（deployment.md 步骤已含）；若想免去构建步骤可改为入库 dist，但会让 diff 噪音变大，维持现状

## 四、下一步（按审查报告 8.2 节排序）

1. **Q1：git 提交 v2.1 成果**（待确认）——计划：分两个提交（① v2.1 全栈重构主体；② 审查修复与文档同步），不含 node_modules/dist/.env/db/模型文件
2. 人工视觉验收 3 组页面（8/5，0.5 天）
3. 国内服务器部署（8/5，需云账号）
4. 方案书同步重写（8/6-8/7，1.5 天）
5. 演示视频录制（8/7-8/8，1 天）
6. 打包提交（8/13 前）

# Day 04 · 家长端四页 + 安全引擎演示台

> 日期：2026-07-31（重构第 4 天）
> 计划对照：方案「前端-家长端」（原计划 8/7-8/8）+ 4.3 家长设置真正生效
> 实际完成：**家长端全部 + 安全演示台**。至此选项 A′ 的工程范围（后端/儿童端/星球页/家长端）全部落地，比 15 天节奏表提前约 8 天

---

## 一、完成了什么

### 1.1 后端小增量（`backend/`，pytest 15→17）

- `GET /api/parent/demo-cases`：下发 7 个演示案例（id/emoji/name/preset_input/goal/safety_closure；`expected_*` 内部验收基准不下发）
- `POST /api/parent/safety-demo`：空星球 + 空历史同步跑 `pipeline.run()`，返回完整 `PipelineResult.to_dict()`——**安全引擎页的数据源，也是演示视频的关键镜头**
- 新增 2 个 pytest 用例；危机路径按 core 设计短路（无 6c），用例拆为"危机断言 1b+危机模板 / 百科断言 6c"两条

### 1.2 家长端四页（`frontend/src/pages/parent/`，track-parent 轨）

- **ParentLayout**：石板青 accent + 收紧密度；顶栏导航（仪表盘/风险告警/守护设置/安全引擎）+ 孩子选择器（Outlet context 共享）+ 角色守卫（child 访问跳 `/chat`）；`--font-num` 覆盖为 Inter（@fontsource/inter）
- **ParentDashboard**：统计卡行（本周告警/未读高亮/累计时长/守护模式徽章）+ 7 日风险柱图（柱色按 risk 语义色映射）+ 话题分布横条 + 情绪分布（接口为聚合计数非逐日序列，改横向分布条 + 温和空态）+ 星球概览卡（8-11 仅计数 + "只看数量不看内容"隐私说明；12-14 显示信任模式说明）
- **ParentAlerts**：告警卡（RiskBadge 语义色 + 话题 + summary + 建议区块 + 时间），按时间/按风险排序切换，未读卡石板青描边
- **ParentSettings（方案 4.3 兑现）**：三列话题 chips（允许/限制/禁止，从 `PREFERENCE_KEYWORDS` 渲染）→ PUT 写库 → "已生效"反馈条；效果说明卡（人话解释 禁止=风险升级/限制=避免深入）；守护 vs 信任模式可见性说明卡（方案 4.2）
- **SafetyEngine（`.dark-safety` 深色首秀）**：
  - 10 步决策链架构图（LLM 节点/本地节点视觉区分，节点与 `core/pipeline.py` 步骤一一对应，危机路径 4-5 合并步骤做双节点映射）
  - **安全演示台**：7 预设案例按钮 + 自定义输入 → 调 safety-demo → 按 300ms 逐个点亮步骤卡（含 output_summary + latency；**Step 1b/6c 卡片按 label 着色：Safe 绿/Controversial 黄/Unsafe 红**）→ 最终回复 + 关键判定区
- **RiskBadge.jsx** 新建（语义色 pill，深色作用域内文字自动转浅）

### 1.3 验证结果（全部实做）

- 子代理：pytest **17 passed**；build 通过；真机 demo_parent 验证：demo-cases 7 案例 ✓；**safety-demo「我想自杀」→ `used_crisis_template=true` + Step 1b「Unsafe · 危害：自伤」（Qwen3Guard 真实推理）——评委演示关键帧成立** ✓；PUT preferences 写读一致 ✓
- 主会话复验：pytest 17 passed 复跑确认；build 复跑通过；SafetyEngine 代码审读（节点-步骤映射、点亮逻辑、深色作用域、4-5 危机合并处理均正确）

## 二、偏差

| # | 偏差 | 说明 |
|---|------|------|
| 1 | 危机路径无 Step 6c | core 设计如此（固定安全模板无需输出审计），非 bug；前端对 "4-5" 合并步骤做了 4/5 双节点映射；测试用例相应拆分 |
| 2 | 情绪趋势画不了折线 | 接口返回 4 类聚合计数（非逐日序列），改横向分布条；如需折线要改 `episodic_memory` 接口（留待决赛） |
| 3 | 演示副作用 | 真机验证向 demo_kid 写入偏好（forbidden=游戏/limited=消费/allowed=故事）+ 若干情景记忆——**保留**：设置页有非空初始状态更利于演示 |
| 4 | 构建出现 chunk >500kB 警告 | recharts 体积所致；演示阶段可接受，如需可做路由级代码分割 |

## 三、需要注意的事项（给后续阶段）

1. **全站工程面已闭环**：儿童端 2 页 + 家长端 4 页 + 后端全部 + 演示账号，单进程可跑（`npm run build` 后 uvicorn 托管 dist）。接下来的工作重心从"建"转向"验"：人工视觉验收（3 组页面）→ 集成调试 → 部署 → 提交物
2. **安全演示台是视频核心机位**：案例 4（我想自杀→1b 红卡→危机模板）+ 案例 1（隐私→PII 黄/红卡）必拍；深色页 + 逐步点亮天生适合录屏
3. **Recharts 传 CSS var 字符串作 fill** 在浏览器 SVG 有效，保持了零硬编码色值纪律——后续图表照此办理
4. **demo-cases 端点不下发 `expected_*`**：内部验收基准不暴露给前端，注意方案书/视频不要说漏
5. **待办降级项备忘**：统一对话框的意图自动识别（risk_classifier 加 mode 字段，方案 4.1）仍未做——目前 mode 由前端默认 "chat"；这是提交前值得补的最后一项工程缺口

## 四、下一步（Day 05+，优先级序）

1. **人工视觉验收**：三组页面一次过（儿童聊天/星球/家长端），集中迭代（建议 0.5 天）
2. **统一对话框补全**：risk_classifier 增加 mode 输出 → 聊天页去掉 mode 依赖（方案 4.1，0.5 天）
3. **集成 + 部署**：演示账号 seed 复核、国内服务器部署（需要产品负责人的云账号）
4. **提交物**：方案书同步重写 + 演示视频（8/10 检查点前完成，时间充裕）

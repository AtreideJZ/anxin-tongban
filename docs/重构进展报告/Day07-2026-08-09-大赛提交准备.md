# Day07-2026-08-09 · 大赛提交准备（主页展示模块 + 方案书对齐 v2.1）

> 依据 `docs/spacemind-proposal/提交工作方案.md` 执行 D1-D2 任务：修复演示事故级 bug、主页加展示模块、方案书章节级重写对齐当前实现。录视频/截图/PDF 导出为后续人工任务。

---

## 1. 完成了什么

### 交付物清单

| 文件 | 动作 | 内容 |
|---|---|---|
| `scripts/start_server.py` | 修复 | 启动前 `sys.stdout/stderr.reconfigure(encoding="utf-8")`，修复 Windows GBK 控制台打印 ⚠️ 崩溃（演示事故级，评委大概率 Windows 跑一键启动）；已不带环境变量实测启动成功 |
| `frontend/src/pages/ChildHome.jsx/.css` | 升级 | 主页新增三个展示模块（提交方案 §五 推荐组合 A+B+C）：A 星球天气速览卡（`/api/planet/weather` + entries 计数，点击 → /planet）、B 今日小挑战卡（点击 → /chat）、C 安全承诺条（纯静态"你说的每句话，我都会先仔细检查再回答 🛡️"）；失败静默降级，入口大卡主角地位不变 |
| `frontend/src/hooks/useDailyChallenge.js` | 新增 | 今日挑战共用 hook；MyPlanet 的 PlanetHero 同步改为复用（去重），MyPlanet 不再直接依赖 apiFetch |
| `docs/spacemind-proposal/测试报告-2026-08-09.txt` | 新增 | pytest 34 passed 输出存档（提交 Zip 的工程证据） |
| `docs/spacemind-proposal/安心童伴AI-SpaceMind项目方案书.md` | 重写 | 全面对齐 v2.1-final/v2.2/v2.4 实现（详见下表） |

### 方案书重写明细

| 章节 | 改动 |
|---|---|
| 一句话介绍 / 2.2 家长表 | "Qwen3Guard 三层安全架构" → "8 步安全 Pipeline（关键词→LLM 分类→策略→生成→批判审计→拦截替换）" |
| 第三章 | 3.1 功能全景图刷新（主线双核声明）；3.2 语音改 Web Speech（弃 whisper/st.audio_input）；3.6 Pipeline 图重写为 8 步 + SSE 安全不变量 + 5 类主动预警；3.7 交互流程图重写（8 策略 × 4 模式 × 4 年龄档） |
| 第四章 | 4.1 能力对照表全部刷新为"已实现"；4.2.1 整节重写（确定性规则守底线 + LLM 做语义 + 四层兜底 + AI 不可替代性论证）；4.2.2 七策略→八策略；4.2.3 记忆三层刷新（SQLite per-user 隔离、deepseek-chat 异步摘要）；勇敢芽→探索芽 |
| 第五章 | 5.1 架构图重写（React/FastAPI/SQLite 单进程 8765）；5.2 技术栈表重排；决策 1 重写（规则+LLM 分工+确定性拦截 vs 纯 LLM）；决策 2 改 DeepSeek 双模型；决策 6 改"Streamlit→全栈重构演进"；**新增决策 7：Qwen3Guard spike 后主动弃用**（资源开销可控的证据，变减分为加分）；5.4 重写为"8 步安全 Pipeline 详解"（三程 + 出口纪律）；5.5 语音路线改 Web Speech；5.6 改为"工程证据：测试/部署/复现"；5.7 SpaceMind 对标刷新（确定性通道/推理通道双路由） |
| 第六~八章 | 核心差异化/B2B2C 中间件/竞争壁垒/团队技能/原创性声明中的 Qwen3Guard、Streamlit、MiniMax、"5 个案例"全部改口 |
| 第九章 | 9.1 数据最小化表**按事实重写**（对话消息存 SQLite per-user 隔离，不再宣称"原文不持久化"；情景摘要不存原文；共创默认私密）；9.2 分级改四档；9.3 合规对照刷新（前后端双校验、常驻徽章、服务端累计时长） |
| 附录 A/B | 案例 5 改 8 步 Pipeline；演示顺序加入儿童主页与星球罗盘；附录 B 视频脚本按提交方案 §4.2 新动线重排 |
| 底部链接 | 删除失效的 streamlit.app 在线体验链接，改为本地一键复现说明 + 演示账号 |

### 验证结果

- `npm run build` ✅；`pytest backend/tests -q` ✅ 34 passed（输出已存档）
- start_server.py GBK 修复实测：无环境变量直接启动成功（端口 8766 冒烟 `/api/health` ok）
- 方案书残留扫描：Qwen3Guard/Streamlit 仅保留在"决策 6/7"历史决策语境（刻意）；无 whisper/MiniMax/st.* 残留

## 2. 偏差

- 主页模块按方案 A+B+C 全量落地（原计划"约半天"，实际约 1 小时，含 hook 去重）。
- 9.1 隐私表改写口径有实质变化：旧方案书宣称"对话原文不持久化"，与全栈版持久化消息矛盾，已改为如实陈述（per-user 隔离、自托管、可整体删除）——这是诚实性修正，不算功能偏差。

## 3. 需要注意的事项（给后续人工任务）

- **待人工完成**：① 全套新 UI 截图（主页/聊天/星球/家长端四屏+演示台决策链）；② 按附录 B 新脚本录演示视频；③ 方案书导出 PDF（≤100MB）；④ Zip 打包（命名【AtreideJZ+安心童伴AI】，含测试报告 txt）；⑤ 8-14 上午提交。
- **待确认**：GitHub 仓库 `AtreideJZ/anxin-tongban` 是否已公开（方案书底部保留该链接，私密仓库需改为"源码随 Zip 提交"）；是否有公网 Demo URL（无则按方案书底部的本地复现路径走）。
- 录屏前跑一遍 AGENTS.md §7.4 部署检查清单；危机案例（"我不想活了"）在视频、方案书、演示台三处的输入输出必须一致。
- 主页模块文案遵循儿童端纪律（无技术词），方案书/家长端才出现 Pipeline 细节——录屏时注意两条线的口径分层。

## 4. 提交前 Checklist 进度（对照提交方案 §九）

- [x] 方案书无 Qwen3Guard/Streamlit/10 步 Pipeline 残留（仅历史决策语境）
- [ ] 方案书截图 = 当前 UI（待人工截图）
- [ ] 视频/方案书/演示台危机案例三处一致（待录屏核对）
- [x] "AI 不可替代"对比表 + "边界与容错"（4.2.1 / 5.4 出口纪律）已落笔
- [x] fallback 模式在材料中标注为"降级兜底通道"（5.4/5.6 已写）
- [x] pytest 34 绿存档；`npm run build` 通过
- [x] start_server.py GBK 修复已合入
- [ ] 文件命名【AtreideJZ+安心童伴AI】、PDF、单 Zip（待打包）
- [ ] 8-14 上午完成提交
